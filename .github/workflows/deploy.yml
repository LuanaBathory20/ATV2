
name: Deploy to Vercel

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install Vercel CLI
      run: npm i -g vercel

    - name: Deploy to Vercel
      run: vercel --token ${{ secrets.aujguiNR0KcEu0pjKrTSWjm6 }} --prod --token # Se der erro, pode tentar colocar --yes
      env:
        VERCEL_ORG_ID: ${{ secrets.VNYOfK6FxygWdi6VnRkRqZQw }}
        VERCEL_PROJECT_ID: ${{ secrets.prj_hkJXOO1uEmBPmeq8oqoJneQWAYLy }}

